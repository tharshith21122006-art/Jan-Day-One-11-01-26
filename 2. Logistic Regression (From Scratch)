import math

X = [0,1,2,3]
Y = [0,0,1,1]
w = b = 0
lr = 0.1

def sigmoid(z):
    return 1/(1+math.exp(-z))

for _ in range(1000):
    for x,y in zip(X,Y):
        y_pred = sigmoid(w*x + b)
        error = y - y_pred
        w += lr*error*x
        b += lr*error

print(w,b)
